<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zmh.service_user.mapper.UserMapper">

    <select id="panduan" resultType="int" parameterType="String">
        select count(1) from user where username= #{user};
    </select>

</mapper>